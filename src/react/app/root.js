import React, {useEffect} from "react";
import("./root.css")

export const ChangeThemeContext = React.createContext({
    toggleColorMode: () => {},
})

function Root(props) {
    
    return (
        <div>
            !!!!!!!!!!!!!!!!!!!!!!
        </div>
    );
}

export default Root;
